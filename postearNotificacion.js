exports.handler = async (event) => {
    // TODO implement
    const response = {
        statusCode: 200,
        body: JSON.stringify('Tu pedido está en camino.'),
    };
    return response;
};
